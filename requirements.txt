# OpenSearch Natural Language Query Parser Requirements

# Core parser - no dependencies (uses only Python standard library)

# Optional: Full OpenSearch client functionality
opensearch-py>=2.3.0  # For connecting to and querying OpenSearch clusters
requests>=2.25.0      # HTTP client (usually comes with opensearch-py)

